<%= form_with(model: camp_instructor, local: true) do |form| %>
  <% if camp_instructor.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(camp_instructor.errors.count, "error") %> prohibited this camp_instructor from being saved:</h2>

      <ul>
      <% camp_instructor.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :camp_id %>
    <%= form.number_field :camp_id, id: :camp_instructor_camp_id %>
  </div>

  <div class="field">
    <%= form.label :instructor_id %>
    <%= form.number_field :instructor_id, id: :camp_instructor_instructor_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
